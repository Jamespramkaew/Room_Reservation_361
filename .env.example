# Used by docker-compose.yml (copy to .env)

# Free Hobby account token: https://app.localstack.cloud/workspace/auth-token
LOCALSTACK_AUTH_TOKEN=

# Host port for Postgres (change if 5432 is already used on your machine)
DB_HOST_PORT=5432

# Set to 1 for verbose LocalStack logs
LOCALSTACK_DEBUG=0
